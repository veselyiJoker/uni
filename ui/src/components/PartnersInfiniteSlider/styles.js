import styled from 'styled-components'
import Slider from 'react-slick'

export const StyledPartnersInfiniteSlider = styled(Slider)`
    position: relative;
    width: 100%;
    overflow: hidden;

    .slick-track {
        display: flex;
    }
`
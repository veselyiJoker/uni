import styled from 'styled-components'
import {
    List,
    ListItemButton,
    Collapse
} from '@mui/material'


export const NomenclatureList = styled(List)`

`

export const NomenclatureSubList = styled(List)`

`

export const CollapseContainer = styled(Collapse)`

`

export const NomenclatureListItem = styled(ListItemButton)`

`